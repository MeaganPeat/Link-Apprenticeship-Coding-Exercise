var expect = require("chai").expect;
var tools = require("../tools");

describe("calculateCosts()", function(){
    it("should calculate the final cost", function(){
        var results = tools.costCalculator();
    });
});